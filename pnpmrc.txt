# pnpm configuration for Bitcoin Blocks

# Allow legacy peer dependencies to avoid conflicts
legacy-peer-deps=true

# Auto install peer dependencies
auto-install-peers=true

# Don't fail on peer dependency warnings
strict-peer-dependencies=false

# Use hoisted node_modules structure for better compatibility
node-linker=hoisted

# Shamefully hoist to handle React 18 compatibility
shamefully-hoist=true

# Public hoist pattern for common packages
public-hoist-pattern[]=*@radix-ui/*
public-hoist-pattern[]=*framer-motion*
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*@farcaster/*
