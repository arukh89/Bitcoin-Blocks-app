This directory contains generated TypeScript bindings for the SpacetimeDB module.

Do not edit files here manually. Regenerate them with:

spacetime generate \
  --lang typescript \
  --out-dir src/spacetime_module_bindings \
  --project-path spacetime-server

Targets: maincloud or your configured server via `spacetime server set`.
