"use client"
import { useEffect } from "react"

export function useQuickAuth(_isInFarcaster: boolean) {
  useEffect(() => {
    // Placeholder: hook intentionally does nothing to avoid build errors
  }, [_isInFarcaster])
}
